[phases.setup]
nixPkgs = ["python3", "nodejs_22", "ffmpeg"]

[phases.install]
cmds = ["python3 -m venv /app/.venv && /app/.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/app/.venv/bin/python bot.py"
