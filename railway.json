{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y nodejs ffmpeg python3-pip && /usr/bin/python3 -m pip install --break-system-packages --upgrade --no-cache-dir -r requirements.txt"
  },
  "deploy": {
    "startCommand": "/usr/bin/python3 bot.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}